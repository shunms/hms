/*****************************************************************************
 * hotel_base.h - header file
 *
 * Author: Savitha Patil <patilsavitha@yahoo.com>
 * Team: Softlinks <http://softlinks.nile.homelinux.net:8000>
 *
 * Created: 2006-04-16
 *
 ******************************************************************************/



#ifndef HOTEL_INFO_BASE_H
#define HOTEL_INFO_BASE_H


#include <QObject>


/**
* The Hotel_Info_Base class provides the foundation for the
* data attributes of the Hotel_Info_Base class.
*/
class Hotel_Info_Base //: public QObject
{
    //Q_OBJECT

    public:

        /**
        * Constructor for Hotel_Info_Base class.
        */
        Hotel_Info_Base( );

		/**
        * Destructor for Hotel_Info_Base class.
        */
        ~Hotel_Info_Base( );

        /**
        * Sets the hotel name.
        */
        void SetName( QString name );

        /**
        * Sets the address part of the hotel address.
        */
        void SetAddress( QString address );

        /**
        * Sets the city part of the hotel address.
        */
        void SetCity( QString city );

        /**
        * Sets the state part of the hotel address.
        */
        void SetState( QString state );

		/**
        * Sets the zip code of the hotel address.
        */
        void SetZipCode( QString zipCode );

        /**
        * Sets the country part of the hotel adderess.
        */
        void SetCountry( QString country );

		/**
        * Sets the phone mumber of the hotel.
        */
        void SetPhone( QString phone );

        /**
        * Sets the email id of the hotel.
        */
        void SetEmail( QString email );

		/**
        * Sets the url of the hotel.
        */
        void SetUrl( QString url );

        /**
        * Sets the notes for the hotel.
        */
        void SetNotes( QString notes );

        /**
        * Sets the # of rooms in the hotel.
        */
        void SetNumOfRooms( int number );

        /**
        * Sets true/false(availability) of restaurants in the hotel.
        */
        void SetRestaurants( bool restaurants );

        /**
        * Sets true/false(availability) of function rooms in the hotel.
        */
        void SetFuncRooms( bool funcRooms );

        /**
        * Sets true/false(availability) of Golf Course in the hotel.
        */
        void SetGolfCourse( bool golfCourse );

        /**
        * Sets true/false(availability) of Tours in the hotel.
        */
        void SetTours( bool tours );

        /**
        * Sets true/false(availability) of shuttles in the hotel.
        */
        void SetShuttles( bool shuttles );

        /**
        * Sets true/false(use) of gift certificates in the hotel.
        */
        void SetGiftCertfs( bool giftCertfs );



		/**
        * Gets the hotel name.
        */
        QString GetName( );

        /**
        * Gets the address part of the hotel address.
        */
        QString GetAddress( );

        /**
        * Gets the city part of the hotel address.
        */
        QString GetCity( );

        /**
        * Gets the state part of the hotel address.
        */
        QString GetState( );

		/**
        * Gets the zip code of the hotel address.
        */
        QString GetZipCode( );

        /**
        * Gets the country part of the hotel adderess.
        */
        QString GetCountry( );

		/**
        * Gets the phone mumber of the hotel.
        */
        QString GetPhone( );

        /**
        * Gets the email id of the hotel.
        */
        QString GetEmail( );

		/**
        * Gets the url of the hotel.
        */
        QString GetUrl( );

        /**
        * Gets the notes for the hotel.
        */
        QString GetNotes( );

        /**
        * Gets the # of rooms in the hotel.
        */
        int GetNumOfRooms( );

        /**
        * Gets true/false(availability) of restaurants in the hotel.
        */
        bool GetRestaurants( );

        /**
        * Gets true/false(availability) of function rooms in the hotel.
        */
        bool GetFuncRooms( );

        /**
        * Gets true/false(availability) of Golf Course in the hotel.
        */
        bool GetGolfCourse( );

        /**
        * Gets true/false(availability) of Tours in the hotel.
        */
        bool GetTours( );

        /**
        * Gets true/false(availability) of shuttles in the hotel.
        */
        bool GetShuttles( );

        /**
        * Gets true/false(use) of gift certificates in the hotel.
        */
        bool GetGiftCertfs( );


    private:
        QString hotelName;
        QString hotelAddress;
        QString hotelCity;
        QString hotelState;
        QString hotelZipCode;
        QString hotelCountry;
		QString hotelPhone;
        QString hotelEmail;
        QString hotelUrl;
        QString hotelNotes;
        int		hotelRooms;
		bool	hotelRestaurants;
		bool	hotelFuncRooms;
		bool	hotelGolfCourse;
		bool	hotelTours;
		bool	hotelShuttles;
		bool	hotelGiftCertfs;

};



#endif

// EOF

