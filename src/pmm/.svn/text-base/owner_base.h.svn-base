/*****************************************************************************
 * Owner_base.h - header file
 *
 * Author: Savitha Patil <patilsavitha@yahoo.com>
 * Team: Softlinks <http://softlinks.nile.homelinux.net:8000>
 *
 * Created: 2006-04-23
 *
 ******************************************************************************/



#ifndef OWNER_INFO_BASE_H
#define OWNER_INFO_BASE_H


#include <QObject>


/**
* The Owner_Info_Base class provides the foundation for the
* data attributes of the Owner_Info_Base class.
*/
class Owner_Info_Base //: public QObject
{
    //Q_OBJECT

    public:

        /**
        * Constructor for Owner_Info_Base class.
        */
        Owner_Info_Base( );

		/**
        * Destructor for Owner_Info_Base class.
        */
        ~Owner_Info_Base( );

        /**
        * Sets the owner first name.
        */
        void SetFirstName( QString firstName );

        /**
        * Sets the owner last name.
        */
        void SetLastName( QString lastName );

        /**
        * Sets the address part of the owner address.
        */
        void SetAddress( QString address );

        /**
        * Sets the city part of the owner address.
        */
        void SetCity( QString city );

        /**
        * Sets the state part of the owner address.
        */
        void SetState( QString state );

		/**
        * Sets the zip code of the owner address.
        */
        void SetZipCode( QString zipCode );

        /**
        * Sets the country part of the owner adderess.
        */
        void SetCountry( QString country );

		/**
        * Sets the phone mumber of the owner.
        */
        void SetPhone( QString phone );

        /**
        * Sets the email id of the owner.
        */
        void SetEmail( QString email );

        /**
        * Sets the notes for the owner.
        */
        void SetNotes( QString notes );


        /**
        * Gets the owner first name.
        */
        QString GetFirstName( );

        /**
        * Gets the owner last name.
        */
        QString GetLastName( );

        /**
        * Gets the address part of the owner address.
        */
        QString GetAddress( );

        /**
        * Gets the city part of the owner address.
        */
        QString GetCity( );

        /**
        * Gets the state part of the owner address.
        */
        QString GetState( );

		/**
        * Gets the zip code of the owner address.
        */
        QString GetZipCode( );

        /**
        * Gets the country part of the owner adderess.
        */
        QString GetCountry( );

		/**
        * Gets the phone mumber of the owner.
        */
        QString GetPhone( );

        /**
        * Gets the email id of the owner.
        */
        QString GetEmail( );

        /**
        * Gets the notes for the owner.
        */
        QString GetNotes( );


    private:
        QString ownerFirstName;
        QString ownerLastName;
        QString ownerAddress;
        QString ownerCity;
        QString ownerState;
        QString ownerZipCode;
        QString ownerCountry;
		QString ownerPhone;
        QString ownerEmail;
        QString ownerNotes;


};



#endif

// EOF

